import { PersonT } from "./output-types";

const database: Array<PersonT> = [ { firstName: 'Christian', lastName: 'Martinet' }, { firstName: 'Baptiste', lastName: 'Martinet' } ];

export default database;